# 《听你的呀》接口文档（API Reference）

> 版本：对应代码库 v3.0.0 ｜ 编写日期：2026-08-17
> 接口载体：微信云函数 loveApi（action 分流），唯一后端入口
> 配套：软件设计架构说明.md（数据结构/权限/状态机）

---

## 〇、通用规范

### 调用方式

所有接口 = 前端 wx.cloud.callFunction 调用云函数 loveApi，入参必须带 action 字段：

```js
wx.cloud.callFunction({
  name: "loveApi",
  data: { action: "queryEvents", spaceId: "sp_xxx" },
});
```

### 返回格式

统一为 JSON：成功 { success: true, ...业务字段 }；失败 { success: false, msg: "原因" }；异常兜底 { success: false, msg: e.message || "error" }。

### 身份与鉴权

- 身份：云函数内 cloud.getWXContext().OPENID，前端不用传 openid；
- 空间校验：凡带 spaceId 的接口都校验 openid ∈ space.members，否则「无权操作/无权查看」；
- 角色校验：appendEvent/redeemCard 按角色权限表校验（见架构说明 §3.4 / §3.9）；
- 无 token/session/JWT，无自建限流。

### 事务

写接口（joinSpace/appendEvent/markRead/updateSpace/redeemCard）全部使用 db.runTransaction，事件写入与分数/未读变更同生共死。

### 内容安全与字数

- msgSecCheck 覆盖：joinSpace（spaceName/guideName/respondName）、appendEvent（text）、updateSpace（纪念日 name）；明确违规才拦，接口故障重试 1 次后放行；
- 字数：非 initiate 事件 text ≤ 50 字（后端强校验，超限「字数超限」）；initiate 存 JSON 不受此限。

---

## 一、genInviteCode —— 生成邀请码

| 项 | 说明 |
|---|---|
| 权限 | 任意（无空间校验） |
| 入参 | 无 |
| 出参 | { success: true, code: "sp_xxx" } |
| 失败 | { success: false, msg: "没成功，别急，再试一次" }（3 次撞码） |

逻辑：循环 3 次生成 sp_ + 时间戳36进制 + 随机串，查空间集合判重，返回未占用码。

调用方：pages/pair/pair（创建空间确认后）。

---

## 二、joinSpace —— 第二人加入（唯一建空间）

| 项 | 说明 |
|---|---|
| 权限 | 任意；须非自己邀请自己 |
| 事务 | 创建空间 + begin 事件 |

入参：

| 字段 | 类型 | 必填 | 校验 |
|---|---|---|---|
| code | string | ✅ | 邀请码；已存在 →「邀请已经用过了」 |
| guideOpenid | string | ✅ | string 且 ≤64；=== 自己 openid →「不能接受自己的邀请」 |
| spaceName | string | ✅ | msgSecCheck |
| guideName | string | ✅ | msgSecCheck |
| respondName | string | ✅ | msgSecCheck |
| startDate | string | 否 | 空则存 "" |

出参：{ success: true, spaceId: code, role: "respond", guideOpenid }。

失败：参数不完整 / 邀请已经用过了 / 成交失败，请重试 / 内容包含敏感词，请修改 / 不能接受自己的邀请。

调用方：pages/pair/pair（加入确认）。

---

## 三、restore —— 身份恢复

| 项 | 说明 |
|---|---|
| 权限 | 任意（按自己 openid 查） |
| 入参 | 无 |
| 出参 | { success: true, hasSpace: bool, space?, role?, openid } |

逻辑：where(members 含 openid).limit(1) 查空间。无空间 hasSpace=false。

调用方：app.restore()（app.js），被首页/我的/配对/纪念日等复用。

---

## 四、appendEvent —— 落事件（核心）

| 项 | 说明 |
|---|---|
| 权限 | 见下；按 kind 选权限表 |
| 事务 | 事件 + 分数 + 未读 |

入参：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| spaceId | string | ✅ | 空间 _id |
| threadId | string | 视情况 | 约定线程/卡线程；initiate 或卡事件可省略（服务端生成） |
| type | string | ✅ | 事件类型（见架构说明 §2.3） |
| kind | string | 否 | promise / card_activate / card_execute / none |
| text | string | 否 | 文案/JSON；非空过 msgSecCheck，非 initiate ≤50 字 |
| score | number | 否 | 分值 |
| cardType | string | 否 | 卡名（卡事件） |
| moodTags | array | 否 | 心情标签（mood） |

角色权限（requiredActor 不匹配 →「无权操作」）：

- 约定：initiate/confirm/revise/cancel=guide；counter/accept/submit=respond；journal/festivalGrant=不限；
- 卡激活：initiate/cancel/giveup=respond；counter/pause=guide；accept=不限；
- 卡执行：initiate=respond；submit/pause=guide；revise=respond；confirm/cancel=不限。

服务端副作用（同事务）：

1. 幂等：anniversaryGrant/remember 每年每名一次（重复→「今年已处理过」）；festivalGrant 每年每节日一次（重复→「今年已发过」）；
2. threadId：卡 activate 无 tid → card_卡名；initiate 无 tid → spaceId_时间戳；
3. 卡 accept 自动生成文案「{称呼1} 和 {称呼2} 以 {X} 分生效了一张「{卡名}」」；
4. 红点：清操作方自己的未读；非 journal/mood 给另一方标未读；
5. 分数：约定 confirm 按最后谈定分入账（无→「这次约定还没扰定分，先商量好再确认吧」）；anniversaryGrant/festivalGrant +52；卡执行 pause/cancel 退回兑换分。

出参：{ success: true, threadId, eventId }。

调用方：promise(initiate)、detail(全部动作)、index(mood/festivalGrant/anniversaryGrant/remember)、diary(journal)、cards(initiate)。

---

## 五、queryEvents —— 查痕迹

| 项 | 说明 |
|---|---|
| 权限 | 空间成员 |

入参：spaceId（必填）、threadId（可选）、limit（可选，默认 50；页面传 100/1000）。

出参：{ success: true, events: [...] }（createdAt 倒序）。

失败：参数不完整 / 无权查看。

调用方：index/traces/detail/cards/diary/ledger（limit 100~1000）。

---

## 六、markRead —— 灭红点

| 项 | 说明 |
|---|---|
| 权限 | 空间成员 |
| 事务 | 是 |

入参：spaceId、threadId（均必填）。

出参：{ success: true }；未读不存在也成功（幂等）。

调用方：index（点通知卡）、detail（进入即已读）。

---

## 七、updateSpace —— 纪念日整体替换

| 项 | 说明 |
|---|---|
| 权限 | 空间成员 |
| 事务 | 是 |

入参：spaceId（必填）、anniversaries（必填数组）。

校验：数组；每项有 name/date/remindCycle∈{month,year}；name ≤20；name 过 msgSecCheck。违规 →「参数不完整/字数超限/内容包含敏感词，请修改」。

出参：{ success: true, anniversaries }。

调用方：pages/anniversary/anniversary（增/删）。

---

## 八、checkAnniversaries —— 查今天纪念日/节日

| 项 | 说明 |
|---|---|
| 权限 | 空间成员 |

入参：spaceId（必填）。

出参：

```json
{ success: true,
  anniversaries: [{ name, date, remindCycle, days }],
  festivals: [{ name: "七夕" | "情人节" }] }
```

逻辑：只查当天且今年未处理过的；七夕用农历换算（lunar-javascript）。只查不发，发放由前端自动 appendEvent(festivalGrant)。

调用方：pages/index/index（checkReminders）。

---

## 九、redeemCard —— 兑换已生效卡

| 项 | 说明 |
|---|---|
| 权限 | 仅回应方 |
| 事务 | 扣分 + 生成卡执行 initiate |

入参：spaceId（必填）、cardType（必填）。

逻辑：校验成员 → 仅 respond → 查卡 accept 事件拿锁定价（无→「这张卡未生效」）→ 余额校验（不足→「还差 {X} 分。先去完成几个约定，攒够了再兑换。」）→ score -= price → 落 card_execute/initiate（text=卡名，score=price）。

出参：{ success: true, threadId: spaceId_execute_时间戳 }。

调用方：pages/detail/detail（已生效卡「使用心意卡」，前端 redeeming 防连点）。

---

## 十、前端调用点总表

| action | 调用页面 | 场景 |
|---|---|---|
| genInviteCode | pair | 创建空间后生成邀请码 |
| joinSpace | pair | 回应方确认加入 |
| restore | app.js（全局） | 启动/切页恢复身份 |
| appendEvent | promise/detail/index/diary/cards | 一切落事件 |
| queryEvents | index/traces/detail/cards/diary/ledger | 一切查询 |
| markRead | index/detail | 通知已读 |
| updateSpace | anniversary | 纪念日增删 |
| checkAnniversaries | index | 首页提醒 |
| redeemCard | detail | 兑换心意卡 |

---

## 十一、字段字典（事件关键字段速查）

| 字段 | 类型 | 说明 |
|---|---|---|
| kind | string | promise/card_activate/card_execute/none |
| type | string | 17 种事件（见架构说明 §2.3） |
| actor | string | guide/respond/system |
| threadId | string | 约定线程 / card_卡名 / spaceId_execute_ts / null |
| text | string | 纯文案或 JSON（initiate=JSON{title,desc,way}、卡报价=JSON{reason}） |
| score | number | 报价/发放/扣减 |
| cardType | string | 卡名 |
| moodTags | array | 心情 |
| createdAt | number | 时间戳 |

## 十二、限制与注意事项

1. appendEvent 的 confirm 不带 score 也能入账（服务端取谈定分）；前端 confirm 传 score 会被忽略逻辑一致；
2. 卡执行 confirm 不加分、pause/cancel 退分，均服务端保证；
3. queryEvents 无分页游标，limit 上限内全量返回（痕迹页 1000）；
4. 所有写入失败均返回 success:false + msg，前端 toast 优先展示 msg；
5. 云函数改动需在微信开发者工具重新部署后生效。

---

## 2026-08-17 接口与本地数据更新（覆盖旧字段说明）

### 云函数边界

本轮没有新增云函数。现有 `loveApi` 继续负责空间恢复、事件写入、事件查询、已读、纪念日、心意卡和解除空间。

### 心情回应字段

不增加 `replyToType / replyToEventId / replyToMoodTags`。回应心情的随手记复用已有字段：

```json
{
  "type": "journal",
  "threadId": "mood_<moodEventId>",
  "moodTags": ["原心情标签"],
  "text": "回应正文"
}
```

### 本地首页缓存

```text
ting_home_cache_active
ting_home_cache_<spaceId>
```

缓存只保存首页展示快照和时间，不作为权限依据。空间变更、解除或 restore 无空间时清理旧空间缓存。

### 本机语气导入

恢复入口同时接受空间备份和简化语气 JSON：

```json
{
  "type": "copy_pack",
  "copyPack": { "name": "测试·猫爪", "home": {}, "profile": {} }
}
```

语气导入调用 `app.setCopyPack(copyPack)`，保存到 `ting_copy_pack_imported`，选择值为 `imported_v1`；不调用云端，也不覆盖空间数据。`app.getCopy()` 以 `GENTLE_V1` 为基础递归合并导入覆盖项。

### 版本

本地软件版本为 `3.0.0`；备份 envelope 的 `version: 1` 是数据格式版本，两者不可混淆。

