# 2.3-3.0 开发计划

当前版本：v3.0.0

本计划分为三个阶段：

- v2.3：稳定性与“有新的回音”优化
- v2.4：印记事件流与记忆价值
- v3.0：语气包与词汇层正式化

原则：2.3 先解决准确性，2.4 增加记忆价值，3.0 再扩展表达能力。

---

## v2.3：稳定性与“有新的回音”优化

目标：让首页首屏更快、状态更准确，减少重复显示和误导。

### T2.3-1：首页事件分类统一

统一定义：

- 进行中的约定
- 待回应的约定
- 未完成的心意卡动作
- 每日心情回应入口

“有新的回音”只显示当前用户需要处理的事件。

排除：

- 已完成
- 已取消
- 已生效
- 已放弃
- 已被处理过的旧回应

不新增云函数，直接复用现有事件流。

### T2.3-2：建立事件状态判断表

为每种事件明确是否进入“有新的回音”，避免各页面各自判断。

| 事件状态 | 是否进入“有新的回音” |
|---|---|
| 对方刚提交约定 | 是 |
| 对方修改约定 | 是 |
| 引导方已确认 | 否 |
| 引导方已驳回 | 否 |
| 对方点击再试一次 | 根据新事件判断 |
| 心意卡待兑换 | 是 |
| 心意卡已兑换 | 否 |
| 心情已回应 | 否 |

### T2.3-3：修复“处理后仍出现”

用户点击某条回音并完成操作后：

1. 本地立即从列表移除；
2. 云端事件写入成功后重新校准；
3. 云端失败时恢复该条并提示；
4. 返回首页时不重新显示旧事件。

### T2.3-4：区分“新回音”和“最近的小事”

- **有新的回音**：需要现在处理的事情。
- **最近的小事**：已经发生过，但不要求处理的内容。

两者不能重复展示同一事件。

### T2.3-5：本地优先加载校准

固定顺序：

```text
读取本地首页缓存
→ 立即渲染
→ 后台读取云端事件
→ 对比事件最新时间
→ 有变化才更新页面
→ 写回本地缓存
```

缓存增加：

- `cacheVersion`
- `lastEventAt`
- `spaceId`
- 当前语气包 ID

不改变事件数据结构。

### T2.3-6：真机一致性测试

至少测试：

- 引导方首次打开
- 回应方首次打开
- 第二次打开
- 双方连续操作
- 事件处理后返回首页
- 云端延迟
- 云端失败
- 切换空间
- 解除空间后重新打开

### T2.3-7：文档和死代码清理

统一清理旧的“今天的新痕迹”“自动提醒”等旧称呼，避免代码、文档、界面名称不一致。

### v2.3 验收标准

- 已完成约定不再进入“有新的回音”；
- 同一条事件不重复出现；
- 操作完成后返回首页立即消失；
- 本地缓存可用于首屏展示；
- 云端刷新不会覆盖错误的本地内容；
- 不新增云函数。

---

## v2.4：印记事件流与记忆价值

目标：让底部“印记”Tab里的事件流不只是流水记录，而是能帮助双方回看关系。

### T2.4-1：印记事件流内容分层

将事件分为：

- 故事开始
- 重要约定
- 完成的小事
- 心意卡兑换
- 心情回应
- 纪念日
- 空间解除前的备份记录

这些内容显示在底部“印记”Tab中。只读取已有事件，不新增事件类型。

### T2.4-2：印记增加时间筛选

现有筛选已经足够，保持：

- 全部
- 约定
- 心意卡
- 随手记
- 心情

不增加“重要时刻”“完成过的事”等第二套分类，不新增筛选按钮。

### T2.4-3：取消重要事件标记

不增加“重要事件”标记。产品不替双方评价哪一段经历更重要，也不要求用户为共同历史做主观标注。

### T2.4-4：从印记生成私密回顾卡（取消）

- 状态：已取消（2026-08-17）
- 原因：不是核心记录功能；优先保证印记完整显示，不保留额外 Canvas 页面。

不再生成额外回顾卡。印记本身就是完整历史，不新增图片生成、保存和分享流程。

### T2.4-5：优化印记空状态
- 状态：已执行（2026-08-17）

- 没有任何记录：故事还没有写下第一行
- 有开始但暂无其他事件：故事正在慢慢发生
- 有事件但筛选为空：这里暂时没有符合条件的记录

### T2.4-6：印记与恢复数据兼容

- 状态：已完成检查（2026-08-17）

备份恢复时保留：

- 原始事件时间
- 原始事件类型
- 原始角色
- 原始昵称
- 原始空间开始日期

不把回顾卡作为独立事件保存。

### 拾光之旅的职责边界

“拾光之旅”不是事件流页面，继续作为备份与恢复入口，负责：

- 备份当前空间；
- 恢复已有备份；
- 导入本地语气包；
- 解除空间后的数据保留与恢复。

v2.4 不在拾光之旅中增加事件时间线或回顾内容。

### v2.4 验收标准

- 印记能按时间完整展示关系记录；
- 不重复生成事件；
- 印记数据来自事件流；
- 不新增云函数；
- 不改变现有约定、心意卡流程；
- 不引入公开分享和社交功能。

---

## v3.0：语气包与词汇层正式化

目标：让同一套功能可以切换不同表达方式，同时不破坏数据和逻辑。

### T3.0-1：确定语气包结构

- 状态：已执行第一阶段（2026-08-17）
- 当前内置语气已统一增加 `schemaVersion: 1`，保留现有文案结构，避免影响现有页面。

每套语气包分为三层：

```text
语气包
├── 基础文案
├── 词汇层
└── 页面动作文案
```

示例：

```json
{
  "id": "gentle_v1",
  "name": "温柔",
  "schemaVersion": 1,
  "vocabulary": {
    "promise": "约定",
    "diary": "随手记",
    "mood": "心情",
    "card": "心意卡",
    "memory": "拾光之旅"
  },
  "copy": {
    "home": {},
    "diary": {},
    "cards": {},
    "profile": {}
  }
}
```

### T3.0-2：建立文案优先级

- 状态：已执行（2026-08-17）

```text
当前语气包页面文案
→ 当前语气包词汇层
→ 温柔语气默认文案
→ 代码兜底文案
```

任何语气包缺失字段，都不能导致页面显示空白。

### T3.0-3：正式纳入页面标题

- 状态：已执行第三轮（2026-08-17）：首页和“我们”页的邀请、心情、日记、统计、主题与语气切换文案已纳入语气包。

以下内容全部从语气包读取：

- 首页模块名
- Hero 副标题
- 约定页面标题
- 心意卡页面标题
- 随手记页面标题
- 拾光之旅标题
- 事件流文案
- 按钮名称
- 空状态文案
- 提示和确认文案
- 解除空间文案

### T3.0-4：增加语气包校验

- 状态：已执行（2026-08-17）

导入前检查：

- 是否有 `id`
- 是否有 `name`
- 是否有 `schemaVersion`
- 是否为合法 JSON
- 字段类型是否正确
- 文案是否为空
- 文案长度是否超限

失败时拒绝导入，并提示具体原因。

### T3.0-5：语气包导入和覆盖规则

- 状态：已执行（2026-08-17）
- 入口规则：只复用“拾光之旅”现有的恢复按钮，剪贴板内容由程序自动识别为 `backup` 或 `copy_pack`。

恢复入口区分：

- `type: "backup"`：恢复空间数据
- `type: "copy_pack"`：导入语气包

导入语气包时：

- 覆盖当前选中的语气；
- 不修改事件；
- 不修改角色；
- 不修改积分；
- 不修改空间关系；
- 只保存在本地。

### T3.0-6：语气包预览

- 状态：已执行（2026-08-17）

导入前显示：

- 语气包名称
- 词汇示例
- 首页标题示例
- 一个按钮示例
- 当前语气与新语气对比

不需要完整预览所有页面。

### T3.0-7：建立官方内置语气

- 状态：已执行（2026-08-17）

第一批只保留：

- 温柔
- 冷静

其他特殊语气先不内置，避免产品定位和审核风险。

### T3.0-8：主题和语气解耦

- 状态：已完成检查（2026-08-17）

主题控制：颜色、字号、卡片样式、按钮样式。

语气包控制：名称、标题、描述、动作和提示文案。

切换语气不改变颜色，切换主题不改变文字。

### T3.0-9：兼容旧数据

- 状态：已执行（2026-08-17）
- 详情页事件标签和操作标签改为进入页面时读取当前语气包，旧事件仍保留原始结构和原文。

旧事件只保留结构化类型，例如：

```text
promise_create
promise_complete
mood
journal
card_redeem
```

显示文字在渲染时根据当前语气包重新生成，不把旧文案永久写死。

### v3.0 验收标准

- 温柔和冷静可以完整切换；
- 缺失文案不会造成页面空白；
- 语气切换不影响任何事件和数据；
- 备份恢复和语气导入明确区分；
- 不强制依赖云端；
- 现有云函数无需新增。

---

## 总体开发顺序

```text
v2.3-1 事件分类统一
→ v2.3-2 状态判断表
→ v2.3-3 修复重复回音
→ v2.3-4 本地缓存校准
→ v2.3-5 真机测试

→ v2.4-1 印记事件流分层
→ v2.4-2 保留现有印记筛选
→ v2.4-3 取消重要事件标记
→ v2.4-4 取消回顾卡，优先保证印记完整显示

→ v3.0-1 语气包结构
→ v3.0-2 词汇层
→ v3.0-3 全页面接入
→ v3.0-4 导入校验
→ v3.0-5 温柔/冷静双语气
```

---

## 印记 UI 评审记录（2026-08-17）

### 页面定位

- 「印记」是完整历史事件流，不是首页“今天”的提醒模块。
- 「拾光之旅」仍然只负责备份、恢复、语气导入和解除后的数据保留。
- 印记中的历史默认完整展示，不能因为是过去事件就默认折叠。

### 印记最终设计方向

- 按日期分组；
- 同一 `threadId` 的事件合并为一件事情；
- 连接时间线的线条使用固定浅灰色，不跟随主题变化；
- 事件卡片跟随当前主题；
- 只使用少量类型色：约定、心意卡、心情、随手记；
- 结果、动作和原始输入文字同时保留；
- 协商文字、回应文字和心情原文不能被摘要替代；
- 系统自动事件可以弱化，但不能删除；
- 当前先一次读取历史，不做下拉分页，事件量变大后再评估“继续回看更早记录”。

### Tab 图标评审结论

曾讨论并渲染过：

- 今日：小屋；
- 印记：时间线；
- 我们：两个人轮廓；
- 我们：两颗相连的心。

用户明确反馈：两个人和两颗心都觉得丑，因此暂不继续做“我们”图标方案。当前更克制的备选是「我们」只保留文字，不强行添加装饰图标。

本轮图标尝试已撤回，程序当前恢复为纯文字原生 TabBar。

### 已删除的渲染文件

本轮生成的印记多事件、全部事件类型、最终 UI、Tab 图标对比等渲染文件已删除，不作为程序资源保留。渲染图仅用于讨论，不代表最终实现。

### 当前不做

- 不继续设计新的「我们」图标；
- 不把印记做成彩色日志；
- 不默认隐藏历史原话；
- 不增加“重要事件”标记，不替双方评价共同经历；
- 不增加新的 Tab；
- 不为此新增云函数。

---

## v4.0：轻量稳定版

### T4.0-1：发布前稳定性基线
- 状态：已完成静态检查（2026-08-17）
- 关键前端、云函数语法检查通过；`app.json` 可解析；状态测试通过；未发现 `respond` 事件残留。

### T4.0-2：本地语气包管理
- 状态：已执行（2026-08-17）
- 可恢复温柔语气；不增加删除入口；不影响主题、事件、积分和空间。

### T4.0-4：备份恢复安全审计
- 状态：已完成检查（2026-08-17）
- 已确认成员身份、双人角色、当前空间合并、重复导入和语气包隔离均有校验。

### T4.0-5：轻量错误恢复
- 状态：已执行（2026-08-17）
- 首页云端读取失败时保留本地已渲染内容，并提示网络不稳，不清空页面。

### T4.0-6：最终真机验收清单
- 状态：已完成自动检查，待真机点测（2026-08-17）
- 首次打开：结对、角色和首页加载正确；
- 再次打开：先显示本地内容，后台刷新不跳角色；
- 双方视角：引导方与回应方内容不串位；
- 主题切换：颜色变化，文字不变化；
- 语气切换：文字变化，颜色不变化；
- 恢复入口：同一按钮能识别备份和语气包；
- 备份恢复：不覆盖当前空间、不重复事件、不泄露给第三人；
- 断网：已有页面不清空，恢复网络后可重新读取；
- 印记：事件顺序、原文和心情回应引用正确；
- 心意卡与约定：生效、执行、完成和驳回流程无回归。

### T4.0-7：只读页面本地优先
- 状态：已执行第一阶段（2026-08-17）
- 印记、心意卡册、随手记先读取本地缓存，再后台云端刷新。
- 缓存不提供操作权限，不决定最终状态；云端失败时保留已显示内容。
